# Agent Runtime

`@departify/agent-runtime` is Departify's proprietary runtime for managing digital employee lifecycle.

It owns agent registry, lifecycle states, internal messaging contracts, scheduling definitions, permission checks, runtime state, and runtime events.

It does not execute AI models, call model providers, make decisions, expose HTTP APIs, use external queues, or create concrete agents.
