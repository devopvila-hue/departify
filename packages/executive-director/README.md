# Executive Director

Provider-independent orchestration model for the Departify digital company.

This package evaluates intents and produces coordination decisions. It does not execute models, persist data, expose transport APIs, replace Agent Runtime, or call external providers.
