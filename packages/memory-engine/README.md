# Memory Engine

Provider-independent memory model for Departify.

This package defines memory records, context assembly, sessions, lifecycle, retention rules, retrieval contracts, policies, events, and validation. It does not generate vectors, call models, persist data, expose APIs, or use external services.
