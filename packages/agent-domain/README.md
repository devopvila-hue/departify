# Agent Domain

Canonical provider-independent domain model for Departify digital employees.

This package owns only pure domain concepts: aggregate, value objects, lifecycle policy, domain events, and invariants. It does not execute models, coordinate runtime, expose APIs, persist data, or call external integrations.
