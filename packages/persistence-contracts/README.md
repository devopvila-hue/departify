# Persistence Contracts

`@departify/persistence-contracts` defines the official persistence interfaces for Departify.

It contains repository contracts, unit-of-work contracts, transaction contracts, specifications, pagination, filters, optimistic locking, and persistence errors. It does not contain adapters, drivers, migrations, schemas, query implementations, or provider-specific code.
