# Knowledge Engine

Provider-independent knowledge model for Departify.

This package defines documents, chunks, sources, collections, scopes, indexing plans, retrieval contracts, ranking policies, lifecycle, events, and validation. It does not generate embeddings, perform searches, use vector storage, expose APIs, or call external services.
