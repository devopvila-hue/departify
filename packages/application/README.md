# Application Layer

`@departify/application` is the orchestration layer for Departify.

It defines application commands, queries, handlers, DTOs, mappers, validation, services, and application ports. It coordinates the Organization Domain and the Provisioning Engine through contracts only.

It does not contain business rules, persistence, infrastructure adapters, provider SDKs, environment access, transport code, or product features.
