# Architecture

Departify V2 follows a modular monorepo layout with independent application boundaries and package-level capability boundaries.

## Applications

- `apps/portal`: independent portal application.
- `apps/backend`: independent backend application.

### Backend Foundation

`apps/backend` owns the Fastify runtime boundary. Sprint 3 includes only technical infrastructure: configuration loading, logging, centralized error responses, request id propagation, graceful shutdown, health/version endpoints, OpenAPI registration, and tests.

The backend does not contain authentication, users, organizations, persistence, Supabase, business APIs, LLM routing, memory, RAG, plugins, n8n, queues, SSE, or WebSockets.

## Packages

- `packages/auth`: authentication and authorization boundary.
- `packages/config`: centralized typed configuration boundary. Application code must not read `process.env` directly.
- `packages/shared`: cross-cutting shared primitives boundary.
- `packages/logging`: logging and observability boundary.
- `packages/departments`: the **external product unit**. Each Department composes references to existing components — Executive Director (director), Agent Runtime (digital employees), Tool Runtime + Core Tool Catalog (tools), Knowledge Engine (knowledge collections), Memory Engine (memory sessions), Workflows (workflow ids) and, since Sprint 34, Business Discovery (optional discovery id — the Empresa Digital references the discovery of its business) — plus a placeholder for connected applications. Sprint 24 ships the aggregate, the `DepartmentService` composition entry point, lifecycle states (`draft`, `active`, `paused`, `archived`), metrics and the canonical `Comercial` demo with multiple Digital Employees. Sprint 25 adds the `DepartmentTemplate`, the `DepartmentTemplateCatalog` (the only authorised source for initial Departments), the `DepartmentTemplateBuilder` and the canonical `Comercial` template. No new runtimes or bridges; the Department never duplicates logic — it only carries references.
- `packages/llm-router`: only authorized AI model access boundary, with provider-neutral contracts, capability modeling, abstract model catalog, routing policies, requests, responses, validation, an internal `ProviderRegistry`, provider-agnostic observability surface, the `ProviderSelector` policy helper, and the official `LlmRouter` facade (the single operational entry point for AI access in Departify).
- `packages/llm-provider-openai`: first concrete LLM Router provider adapter, implemented with the official OpenAI SDK and configured only through `packages/config`. Exposes `registerOpenAIProvider` so the LLM Router composition can wire it through the registry.
- `packages/llm-provider-google`: Google Vertex AI (Gemini) provider adapter. The only package authorized to import `@google-cloud/vertexai`; exposes `registerGoogleVertexProvider`.
- `packages/llm-provider-minimax`: MiniMax provider adapter using the OpenAI-compatible HTTP API. Exposes `registerMiniMaxProvider`.
- `packages/llm-provider-bridge`: official multi-provider composition boundary. Knows how to assemble OpenAI, Google Vertex and MiniMax into a single Provider Registry call. The bridge is the only place that knows provider names; the router and adapters stay decoupled.
- `packages/rag`: retrieval-augmented generation boundary.
- `packages/knowledge-engine`: proprietary, provider-independent knowledge model boundary for documents, chunks, sources, collections, scopes, abstract indexing, retrieval contracts, ranking policies, lifecycle, events, and validation.
- `packages/memory`: organization-scoped memory boundary.
- `packages/memory-engine`: proprietary, provider-independent memory model boundary for memory records, context assembly, sessions, retention rules, retrieval contracts, lifecycle, policies, events, and validation.
- `packages/plugins`: organization-scoped plugin boundary.
- `packages/platform-composition`: official composition boundary that wires application, provisioning, domain, persistence contracts, and infrastructure adapters. As of Sprint 25 it hosts the `BusinessProvisioningService` that turns a freshly provisioned Organization into an operational company through the DepartmentTemplateCatalog.
- `packages/agent-domain`: canonical, provider-independent Agent domain model for Departify digital employees.
- `packages/agent-runtime`: proprietary, provider-independent runtime for digital employee registry, lifecycle, permissions, internal messaging contracts, scheduling contracts, state, and runtime events.
- `packages/executive-director`: provider-independent orchestration boundary that evaluates intents and produces coordination decisions for the digital company. As of Sprint 23 it is the first decision engine used by `packages/executive-orchestrator` to dispatch deterministic orchestrator intents.
- `packages/executive-orchestrator`: official composition boundary that wires `Executive Director` into the existing runtime flow. Owns the `OrchestratorIntent` types (`health_check`, `organization_summary`, `generate_identifier`, `discovery_analyze`), the `ExecutiveDecisionMapper` (the only component authorised to translate `ExecutiveDecision` into `AgentToolAction`), and the `ExecutiveOrchestrator` facade that runs the pipeline `OrchestratorIntent → ExecutiveDirector → DecisionMapper → AgentToolBridge → Tool Runtime → Core Tool Catalog`. Sprint 30 adds `discovery_analyze` dispatching to the `discovery.analyze` Core Tool, with dynamic Tool args on the intent's `toolArgs` slot. Sprint 31 adds `ExecutiveDiscoveryWorkflow` — the first official Executive workflow — composing Business Discovery initiation (Sprint 28) with the official `discovery.analyze` dispatch (Sprint 30) into a typed `CompanyDiscoveryReport` flow. Every correlation id (intentId → decisionId → actionId) is preserved end-to-end. The package never modifies existing public contracts; it depends on them through their public APIs.
- `packages/application`: application orchestration boundary for commands, queries, DTOs, mappers, handlers, application services, and application ports.
- `packages/organization-domain`: canonical, provider-independent Organization domain model.
- `packages/persistence-contracts`: provider-independent persistence contracts for repositories, unit of work, transactions, specifications, pagination, filters, concurrency, and errors.
- `packages/persistence-supabase`: first concrete persistence adapter, implemented with the official Supabase JavaScript client and configured only through `packages/config`.
- `packages/tool-runtime`: only authorized boundary for executing Tools. Provider-agnostic, host-independent runtime that owns the tool registry, lifecycle, six-phase execution pipeline (`validate → authorize → prepare → execute → observe → complete`), permissions, isolation, timeout, cancellation, limits, scheduling, observability, and the internal event taxonomy (`tool.registered`, `tool.requested`, `tool.started`, `tool.completed`, `tool.failed`, `tool.cancelled`). Real execution is disabled in Sprint 20; the package ships the contracts, registry, pipeline shape and policies only. It does not import provider SDKs, read environment variables, perform HTTP calls, depend on Fastify/Supabase/Docker, or contain product prompts.
- `packages/agent-tool-bridge`: official composition boundary between `Agent Runtime` and `Tool Runtime`. Owns the `AgentToolPort` interface and the `AgentToolRuntimeAdapter` implementation that translates Agent actions into `ToolExecutionRequest` and back. Ships the `system.time` demonstration Tool. The bridge never modifies either runtime; it composes them. No LLM Router, no Executive Director, no Memory Engine, no Knowledge Engine, no plugins, no MCP, no HTTP, no external services.
- `packages/tool-catalog`: official catalog of core Tools. Houses `system.uuid`, `organization.get`, `memory.search`, `knowledge.search`, `system.health`, `discovery.analyze` (deterministic Business Discovery Gap Analysis + Question Generation delegating to `packages/business-discovery`, Sprint 29), `discovery.get` (returns the most recent `CompanyDiscoveryReport` of an organization from a `DiscoveryReportRepository`, Sprint 37), `discovery.readiness` (deterministic go/no-go on Empresa Digital operability, Sprint 41), `discovery.plan` (deterministic first work plan of the Department from the discovery questions, Sprint 42), `discovery.delegate` (deterministic delegation of the plan to the competent Digital Employees, Sprint 43) and `discovery.summary` (deterministic executive summary of the business, Sprint 45). `registerAllCoreTools(registry, context)` is the **single composition point** that wires every catalog Tool into a Tool Runtime registry. The catalog never creates new runtimes, bridges, packages, HTTP clients or SDKs; it depends only on contracts already exposed by `packages/organization-domain`, `packages/memory-engine`, `packages/knowledge-engine`, `packages/llm-router` and `packages/business-discovery` (types only).
- `packages/workflows`: first Department collaboration layer. Owns `WorkflowDefinition`, `WorkflowStep`, `WorkflowExecution`, `WorkflowResult`, `WorkflowBuilder`, the canonical `Lead Qualification Workflow` (`wf_lead_qualification`), the `Business Briefing Workflow` (`wf_business_briefing`, Sprint 40 — the first useful work a freshly provisioned Department performs by reading the organization's Business Discovery report through `discovery.get`), the `Business Readiness Workflow` (`wf_business_readiness`, Sprint 41 — the first automatic decision, a deterministic go/no-go via `discovery.readiness`), the `Department Plan Workflow` (`wf_department_plan`, Sprint 42 — the first automatic planning, ordering the discovery questions via `discovery.plan`) and the `Department Delegation Workflow` (`wf_department_delegation`, Sprint 43 — the first automatic delegation, assigning the plan to the competent Digital Employees via `discovery.delegate`), the `First Work Workflow` (`wf_first_work`, Sprint 44 — the first work executed by a Digital Employee, reading the discovery report via `discovery.get`) and the `First Result Workflow` (`wf_first_result`, Sprint 45 — the first useful result produced by a Digital Employee, the executive summary via `discovery.summary`). Sprint 46 adds `WorkResultRepository` so completed work results are recoverable by the Empresa Digital. Sprint 47 adds `wf_department_onboarding` — the Vending Machine step: a single trigger runs the whole Department pipeline (briefing → readiness → plan → delegation → first work → first result) and delivers the first value. The execution engine dispatches each step through the `AgentToolBridge` (one step = one Digital Employee) and threads the previous step's output into the next step's metadata. Workflows depend only on `AgentToolBridge` and `Tool Runtime`; no IA, no LLM Router, no HTTP, no SDKs. The Comercial department attaches the Lead Qualification Workflow by default.
- `packages/business-events`: official event-driven composition layer. Owns `BusinessEvent`, the five Sprint 27/32/33 events (`lead.created`, `organization.created`, `organization.provisioned`, `organization.discovery_requested`, `organization.discovered`), the `BusinessEventCatalog` mapping event → handler, and the `BusinessEventService` that publishes events through the catalog. The service **never** executes business logic directly: every handler delegates to an existing runtime (`WorkflowExecution`, `BusinessProvisioningService`, `ExecutiveOrchestrator`, `ExecutiveDiscoveryWorkflow`) or a host-supplied reaction (`DiscoveryCompletionHandler`). Idempotent by eventId. The user generates events; Departify decides automatically which department and which workflow should execute.
- `packages/business-discovery`: the only authorized boundary for discovering a company. Sprint 28 introduces the foundation for understanding a business before building its Empresa Digital. Models the complete pipeline without executing AI, scraping, or HTTP calls. Owns `BusinessDiscoveryRequest`, `BusinessDiscoverySession`, `CompanyDiscoveryReport`, `CompanyDNA` (canonical company identity model with mission, vision, values, products, services, market, positioning, strengths, weaknesses, objectives, processes), `FounderBrain` (canonical founder psychology model with leadership style, priorities, philosophy, risk tolerance, delegation, decision making, communication preferences), deterministic `GapAnalysis` (detects missing information without AI), and adaptive `QuestionGenerator` (produces targeted questions via deterministic rules). Sprint 36 adds `DiscoveryReportRepository` (provider-neutral port) with the default `InMemoryDiscoveryReportRepository` so completed reports are recoverable by the Empresa Digital. The pipeline flows: Input → Discovery Session → Company DNA → Founder Brain → Gap Analysis → Questions → Result. No AI, no LLM Router, no HTTP, no SDKs, no Fastify, no Supabase, no Docker.
- `packages/provisioning-engine`: only authorized boundary for future organization creation.

## Platform Boundaries

- Backend and portal are independent.
- Docker is the standard runtime for backend execution.
- Railway runs the backend Docker image and uses `/health` as the deployment healthcheck.
- Netlify is the official portal environment.
- Supabase is the official data platform, initialized by CLI configuration only in the Golden Image.
- The Golden Image installs platform infrastructure only.
- The Provisioning Engine owns future organization provisioning contracts, pipeline phases, and provisioning state.
- The Organization domain owns the canonical aggregate, value objects, lifecycle states, domain events, and pure invariants for representing an organization.
- The Application Layer coordinates application use-case contracts between the Provisioning Engine and Organization domain. It must not own domain rules, persistence, provider SDKs, transport code, or infrastructure adapters.
- Persistence contracts define the stable interface that future storage implementations must satisfy. They do not contain adapters, schemas, migrations, drivers, or provider-specific behavior.
- The Supabase persistence adapter implements the persistence contracts without leaking Supabase row models to domain or application code. Its UnitOfWork provides a shared context; generic database-level atomicity across multiple Data API requests is not guaranteed without a future provider-specific transaction primitive.
- Platform composition owns wiring between existing packages. Sprint 10 uses it for the first real provisioning flow and persists organization, workspace, and provisioning records through the Supabase adapter.
- Agent Domain owns the canonical aggregate, value objects, lifecycle states, domain events, and pure invariants for representing a digital employee. It does not depend on Agent Runtime, provisioning, persistence, transport, provider SDKs, model execution, or infrastructure.
- Agent Runtime owns the lifecycle of Departify digital employees. It manages agent registration, activation, deactivation, listing, lookup, deletion, state transitions, permissions, internal message contracts, scheduling contracts, and runtime events. It does not execute models, make AI decisions, call provider SDKs, expose transport APIs, or own the Executive Director.
- Executive Director owns system-level orchestration intent evaluation and decision creation. It coordinates conceptually through contracts toward Application Layer, Provisioning Engine, and Agent Runtime, but does not execute infrastructure, persist data, call providers, run models, or replace Agent Runtime.
- Future persistence must adapt to `packages/organization-domain`; the domain must not depend on databases, provider SDKs, Docker, Railway, Netlify, Supabase, OpenClaw, or environment variables.
- Agent Runtime is not part of the Golden Image and is not infrastructure.
- LLM Router is the only authorized package for future AI model provider access. Other packages must not import provider SDKs or perform model calls directly. Sprint 14 defines contracts and routing policy models only; it does not implement providers, product prompts, external calls, API keys, or business logic.
- LLM Router is the only operational entry point for AI model access in Departify. Sprint 18 promotes it to the official composition boundary by adding an internal `ProviderRegistry`, a `ProviderSelector` that resolves routing decisions for `capability_first` and `balanced` (and the other strategies already defined), a provider-agnostic observability surface (`RouterLogger` and `RouterMetrics`), and an `LlmRouter` facade that exposes `chat`, `complete`, `embed`, and `stream`. The rest of the system talks exclusively to the facade.
- Departify supports multiple LLM providers through the Provider Registry. Sprint 19 adds `packages/llm-provider-google` (Google Vertex AI / Gemini) and `packages/llm-provider-minimax` (OpenAI-compatible HTTP), plus `packages/llm-provider-bridge` as the single multi-provider composition entry point. Switching providers requires only configuration changes via `packages/config` (`LLM_DEFAULT_PROVIDER` plus the per-provider credentials). No provider SDK leaves its package.
- OpenAI Provider is the first concrete LLM Router adapter. It implements existing provider-neutral router contracts for chat, completion, structured output, tool calling, and streaming without exposing OpenAI SDK types outside `packages/llm-provider-openai`. It consumes API key, default model, timeout, and retry configuration exclusively through `packages/config` and exposes `registerOpenAIProvider` to register itself with the LLM Router's `ProviderRegistry`.
- Google Vertex and MiniMax providers follow the same isolation rules as OpenAI: each is the sole importer of its SDK (or compatible SDK), implements the existing LLM Router contracts without modification, and exposes a `registerXProvider(registry)` helper consumed exclusively by `packages/llm-provider-bridge`.
- Provider Bridge is the only composition entry point that names providers. The router does not import any provider package; it only consumes the `ProviderRegistry` it is handed.
- Tool Runtime is the only boundary allowed to execute Tools. Executive Director decides; Agent Runtime plans; Tool Runtime executes. No other package may invoke a Tool directly. Future integrations (Plugins, MCP, Workflows, external connectors) must talk to Tool Runtime through its contracts.
- Agent Tool Bridge is the only composition point between Agent Runtime and Tool Runtime. Sprint 21 delivers the first runtime-to-runtime integration: an Agent registered through `AgentRegistry` invokes Tools through the `AgentToolPort` interface, the bridge authorizes against Agent permissions, and the Tool Runtime executes the demonstration `system.time` Tool. No IA, no LLM Router, no Executive Director.
- Core Tool Catalog is the official catalogue of platform-level Tools. Sprint 22 ships `system.uuid`, `organization.get`, `memory.search`, `knowledge.search` and `system.health` behind a single composition entry point (`registerAllCoreTools`). Sprint 29 adds `discovery.analyze`; Sprint 37 adds `discovery.get` (reads the most recent discovery report); Sprint 41 adds `discovery.readiness` (deterministic go/no-go on Empresa Digital operability); Sprint 42 adds `discovery.plan` (deterministic first work plan from the discovery questions); Sprint 43 adds `discovery.delegate` (deterministic delegation of the plan to the competent Digital Employees); Sprint 45 adds `discovery.summary` (deterministic executive summary of the business). All Tools use the existing Tool Runtime pipeline and existing Memory / Knowledge / Organization / Business Discovery contracts. No new runtimes, bridges, packages, IA, HTTP or SDKs.
- Executive Orchestration Pipeline. Sprint 23 wires the Executive Director into the runtime flow through `packages/executive-orchestrator`: every deterministic `OrchestratorIntent` (`health_check`, `organization_summary`, `generate_identifier`) produces an `ExecutiveDecision`, which `ExecutiveDecisionMapper` translates into an `AgentToolAction` consumed by `AgentToolBridge`. Sprint 30 adds `discovery_analyze`, which dispatches to the `discovery.analyze` Core Tool (Business Discovery Gap Analysis) through the same pipeline, carrying dynamic Tool args on the intent's `toolArgs` slot. Sprint 31 adds the first official Executive workflow — `ExecutiveDiscoveryWorkflow` — which composes Business Discovery initiation (Sprint 28) with the official `discovery.analyze` dispatch (Sprint 30) into a typed `CompanyDiscoveryReport` flow. No IA, no LLM Router, no HTTP, no SDKs. The pipeline preserves intent/decision/action correlation ids end-to-end.
- External vs internal naming. **The Department is the visible unit of the product. The Digital Employee (mapped to Agent Runtime's `Agent`) is the internal execution unit.** Both coexist: a Department always references at least one Digital Employee; a Digital Employee always belongs to exactly one Department. The Department model is the language the customer sees; the Digital Employee is the language the runtimes speak.
- Department ↔ Discovery. Sprint 34 lets the Department (the Empresa Digital) reference the Business Discovery of its organization (`discoveryId` on the aggregate + `DepartmentService.associateDiscovery`). The Department only carries the reference; the authoritative report lives in `packages/business-discovery` — closing the architecture's declared gap between "understanding a business" and "building its Empresa Digital".
- Business Provisioning Pipeline. Sprint 25 wires Organization provisioning to the DepartmentTemplateCatalog through a new `instantiate_business` pipeline step. After the Organization and Workspace are persisted, the `BusinessProvisioningService` consumes the canonical template (Comercial by default), uses the `DepartmentService` to create the Department, adds Digital Employees, associates Tools, Knowledge Collections, Memory Sessions and Connected Applications, and returns a typed `BusinessProvisioningResult`. The flow is idempotent and reports controlled failures through typed `ProvisioningIssue` entries.
- Department Collaboration. Sprint 26 introduces `packages/workflows` to enable multiple Digital Employees in a Department to collaborate on the same outcome. The `Lead Qualification Workflow` (`wf_lead_qualification`) runs sequentially: Qualify Lead (Lead Qualifier) → Prepare Contact (Outreach Specialist) → Generate Proposal (Proposal Writer). Sprint 40 adds `wf_business_briefing`: a freshly provisioned Department's Sales Director reads the organization's Business Discovery report through `discovery.get` — the first useful work the Empresa Digital performs automatically. Sprint 41 adds `wf_business_readiness`: the Sales Director runs `discovery.readiness` for the first automatic decision (deterministic go/no-go on operability). Sprint 42 adds `wf_department_plan`: the Sales Director runs `discovery.plan` for the first automatic Department planning (discovery questions ordered by priority). Sprint 43 adds `wf_department_delegation`: the Sales Director runs `discovery.delegate` to assign the plan to the competent Digital Employees (first automatic delegation). Sprint 44 adds `wf_first_work`: a delegated Digital Employee runs `discovery.get` to know the business it is about to serve (first work executed by an employee). Sprint 45 adds `wf_first_result`: a delegated Digital Employee runs `discovery.summary` for the first useful result (executive summary of the business). Each step is dispatched through the `AgentToolBridge`; the previous step's output is threaded into the next step's metadata, producing a typed context chain. The Comercial department attaches the canonical workflow by default; the runtime is unchanged.
- Business Events. Sprint 27 introduces `packages/business-events`. The user generates Business Events; the `BusinessEventService` resolves them through the `BusinessEventCatalog` and dispatches them to the existing runtimes. No IA, no reflection, no dynamic discovery. The catalog ships handlers by default: `lead.created → wf_lead_qualification`, `organization.created → business provisioning`, `organization.provisioned → initial activation`. Sprint 32 adds `organization.discovery_requested → ExecutiveDiscoveryWorkflow` (the first official Executive workflow, Sprint 31). Sprint 33 adds the `organization.discovered` fact event so hosts can react to a completed discovery before building the organization's Empresa Digital. Sprint 35 wires a default handler for `organization.discovered` that, when no host completion handler is supplied, associates the completed discovery to the organization's non-archived Departments through the `DepartmentService` (Sprint 34 capability). Sprint 38 makes the `organization.provisioned` handler run the `ExecutiveDiscoveryWorkflow` automatically after a successful activation — completing the end-to-end flow for creating an Empresa Digital from a real company. Sprint 39 makes the `lead.created` handler turn a qualified lead into an organization automatically when an `OrganizationCreator` port is wired. Every event result is idempotent (keyed on eventId) and traces the full correlation chain (eventId, workflowId, executionId, provisioningId, timestamps, errors).
- Memory Engine owns the abstract memory model for Departify. It defines working, episodic, semantic, organization, and agent memory behavior, context assembly, session modeling, retention policies, retrieval contracts, lifecycle rules, and internal events. It does not create vectors, call models, access databases, expose APIs, implement RAG, or depend on provider SDKs.
- Knowledge Engine owns the abstract knowledge model for Departify. It defines documents, chunks, sources, collections, scopes, indexing plans, retrieval contracts, ranking policies, lifecycle rules, and internal events. RAG is a future capability of the Knowledge Engine, not the engine itself. Sprint 16 does not implement embeddings, vector storage, concrete search, APIs, provider SDKs, or external calls.
- Memory, vector data, plugins, and workflows are organization-scoped concerns.
- OpenClaw is not part of the product.
